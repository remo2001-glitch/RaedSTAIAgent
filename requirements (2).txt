# رائد — Institutional Balanced Crypto AI Trading Agent
# requirements.txt — جميع المكتبات المطلوبة

# Telegram Bot
python-telegram-bot[job-queue]==21.6

# HTTP Client (async)
aiohttp==3.9.5
aiofiles==23.2.1

# Data Processing
numpy==1.26.4

# Environment Variables
python-dotenv==1.0.1

# Redis — تخزين دائم عبر Deploys (مطلوب)
redis==5.0.8

# Cryptography — لتشفير API Keys
cryptography==42.0.5

# Logging — مستخدمة في جميع core/ files
loguru==0.7.2

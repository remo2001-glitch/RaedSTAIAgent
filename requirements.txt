# رائد — Institutional Balanced Crypto AI Trading Agent
# requirements.txt — جميع المكتبات المطلوبة

# Telegram Bot
python-telegram-bot[job-queue]==21.6

# HTTP Client (async)
aiohttp==3.9.5
aiofiles==23.2.1

# Data Processing
numpy==1.26.4

# Environment Variables
python-dotenv==1.0.1

# Logging (تُستخدم المكتبة القياسية — لا حاجة لمكتبة إضافية)

# Optional: Redis للكاش المتقدم (اختياري)
# redis==5.0.8

# Optional: Binance API الرسمي (للتنفيذ الحقيقي مستقبلاً)
# python-binance==1.0.19
